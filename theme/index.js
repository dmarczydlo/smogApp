import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1
    },
    background: {
        flex: 1
    },
    header: {
        fontSize: 20,
        fontWeight: 'bold',
        padding: 4,
        textAlign: 'center'
    },
    subHeader: {
        fontSize: 16,
        padding: 4,
        textAlign: 'center'
    },
});

export default styles;
